<Window x:Class="Listings.Views.ProgressBarWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Listings.Views"
        mc:Ignorable="d"
        Title="ProgressBarWindowView" Height="70" Width="500" WindowStyle="None" ResizeMode="NoResize" WindowState="Normal" WindowStartupLocation="CenterOwner" BorderThickness="1" BorderBrush="#CCC">
    
    <Border Padding="10">
        <StackPanel>
            <TextBlock Margin="0 0 0 5" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" />
            <ProgressBar Height="25" Minimum="0" Maximum="100" IsIndeterminate="True" />
        </StackPanel>
    </Border>
</Window>
